import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Tailscale cross-origin warning is informational only
  // No configuration needed - it will be resolved in future Next.js versions
};

export default nextConfig;
